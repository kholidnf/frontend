//Membuat variable URL API: BASE_URL
const BASE_URL = `https://covid-fe-2023.vercel.app/api`;
//MEembuat variable endpoints
const ENDPOINTS = {
    GLOBAL: `${BASE_URL}/global.json`,
    INDONESIAN: `${BASE_URL}/indonesia.json`
}

export default ENDPOINTS;